#include<iostream>
#include<vector>
using namespace std;

vector<int> findSubsetSum(vector<int> &arr, int index, vector<int> ans, vector<int> curr) {
    if(index == arr.size()){
        int sum = 0;
        
    }


}

int main() {
    vector<int> arr = {3,1,2}; 
    vector<int> ans;
    vector<int> curr;
    ans = findSubsetSum(arr, 0 , ans, curr);
}